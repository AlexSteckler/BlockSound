using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SliderAlex : MonoBehaviour
{
    public float[] notes;
    // Start is called before the first frame update
    void Start() {
        notes = new float[8];

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
